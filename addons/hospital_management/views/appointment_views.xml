<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Appointment Form View -->
    <record id="view_hospital_appointment_form" model="ir.ui.view">
        <field name="name">hospital.appointment.form</field>
        <field name="model">hospital.appointment</field>
        <field name="type">form</field>
        <field name="arch" type="xml"> 
            <form string="Hospital Appointment">
                <sheet>
                    <group>
                        <field name="patient_id"/>
                        <field name="appointment_date"/>
                        <field name="note"/>
                    </group>
                </sheet>
                
            </form>
        </field>
    </record>

    <!-- Appointment List View -->
    <record id="view_hospital_appointment_tree" model="ir.ui.view">
        <field name="name">hospital.appointment.tree</field>
        <field name="model">hospital.appointment</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Hospital Appointments">
                <field name="patient_id"/>
                <field name="appointment_date"/>
                       
            </list>
        </field>
    </record>

    <!-- Appointment Action -->
    <!-- TO link views to menu -->
    <record id="action_hospital_appointment" model="ir.actions.act_window">
        <field name="name">Appointments</field>
        <!-- links this action to the hospital.appointment model. -->
        <field name="res_model">hospital.appointment</field>
        <!-- Tells Odoo to open the list view first, and allow switching to form view. -->
        <field name="view_mode">list,form</field>
    </record>
</odoo>